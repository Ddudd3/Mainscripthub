		function makeid(length) {
    var result           = '';
    var characters       = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789$%!&';
    var charactersLength = characters.length;
    for ( var i = 0; i < length; i++ ) {
        result += characters.charAt(Math.floor(Math.random() * charactersLength));
    }
    return result;
}
var ID = localStorage.getItem("ID")
var FID = localStorage.getItem("FID")
var time = new Date().toLocaleTimeString();
var date = new Date().toDateString();

const auth = ["V7Spk&8s$s-hkqFmw9S20"]
var tf = auth.includes(ID)

const banned = []
var btf = banned.includes(ID)
---------------------------------------------------------------
var b = "Banned"
if(btf){
localStorage.removeItem("ID")
localStorage.setItem("ID","banned")
}
if(ID="Banned"){
alert("banned")
}
--------------------------------------------------------
if(ID===null){
var Name = prompt("Enter name. Please Enter it correctly.");
localStorage.setItem("ID",randid)
localStorage.setItem("FID",randid+" -- "+Name+" -- "+time+" -- "+date)
alert("Pending autherization.");
}else{
if(tf){
alert("autherized");
}else{
if(
alert("not autherized"}
}
}
